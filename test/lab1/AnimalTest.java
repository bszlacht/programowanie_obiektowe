// co się stało tym testom?
/*package lab1;

import org.junit.jupiter.api.Test;
import agh.cs.lab1.*;

import static org.junit.Assert.*;


public class AnimalTest{

    @Test
    public void moveTest1(){
        Animal cat = new Animal();
        cat.move(MoveDirection.RIGHT);
        cat.move(MoveDirection.FORWARD);
        cat.move(MoveDirection.FORWARD);
        cat.move(MoveDirection.FORWARD);;
        assertEquals("pozycja: (4,2), orientacja: Wschód", cat.toString());
        // orientacja i pozycja się zgadza

        Animal cat2 = new Animal();
        cat2.move(MoveDirection.RIGHT);
        cat2.move(MoveDirection.FORWARD);
        cat2.move(MoveDirection.FORWARD);
        cat2.move(MoveDirection.FORWARD);
        cat2.move(MoveDirection.FORWARD);
        cat2.move(MoveDirection.FORWARD);
        cat2.move(MoveDirection.FORWARD);

        assertEquals("pozycja: (4,2), orientacja: Wschód", cat2.toString());
        // nie wychodzi poza mapę

        Animal cat3 = new Animal();
        cat3.move(MoveDirection.LEFT);
        cat3.move(MoveDirection.BACKWARD);
        cat3.move(MoveDirection.LEFT);

        assertEquals("pozycja: (3,2), orientacja: Południe", cat3.toString());
        // orientacja i pozycja się zgadza
    }



}*/
